#!/bin/bash

# simple system managment programs, organized

declare -a programs=(
    "btop"
    "nmtui"
    "bluetui"
    "battop"
)

choice=$(printf '%s\n' "${programs[@]}" | dmenu -vi -g 1 -l 20 -i -p "program :") || exit

st -f monospace:20 -c $choice -e $choice
