#!/bin/sh

# Simple dmenu power menu

choice=$(printf "reboot\nshutdown\nlock" | dmenu -i -vi -g 1)

case "$choice" in
    lock)
        sleep 0.1; slock
        ;;
    reboot)
        systemctl reboot
        ;;
    shutdown)
        systemctl poweroff
        ;;
    *)
        ;;
esac

